# ============================================================================
# 판매관리 시스템 환경 설정 템플릿
# ============================================================================
#
# 주의사항:
# 1. 이 파일을 .env로 복사한 후 실제 값으로 변경하세요
# 2. .env 파일은 절대 Git에 커밋하지 마세요
# 3. 각 업체마다 고유한 비밀번호를 사용하세요
#
# ============================================================================

# ----------------------------------------------------------------------------
# 데이터베이스 설정
# ----------------------------------------------------------------------------
DB_USER=sa
DB_PASSWORD=CHANGE_THIS_TO_SECURE_PASSWORD
DB_SERVER=localhost
DB_DATABASE=YmhDB
DB_PORT=1433
DB_ENCRYPT=false
DB_TRUST_SERVER_CERTIFICATE=true

# ----------------------------------------------------------------------------
# 서버 설정
# ----------------------------------------------------------------------------
PORT=3000
NODE_ENV=production

# 베이스 경로 설정 (배포 시 폴더명)
# 접속 URL: http://localhost:3000{BASE_PATH}/index.html
# 예시:
#   - /sales-management-api → http://localhost:3000/sales-management-api/index.html
#   - /erp → http://localhost:3000/erp/index.html
#   - /company-system → http://localhost:3000/company-system/index.html
BASE_PATH=/sales-management-api

# ----------------------------------------------------------------------------
# 회사 정보 (선택사항)
# ----------------------------------------------------------------------------
COMPANY_NAME=회사명
COMPANY_CODE=COMP001

# ----------------------------------------------------------------------------
# 보안 설정
# ----------------------------------------------------------------------------
# 세션 비밀키 (랜덤 문자열로 변경하세요)
SESSION_SECRET=CHANGE_THIS_TO_RANDOM_STRING_AT_LEAST_32_CHARS

# JWT 비밀키 (향후 사용 예정)
JWT_SECRET=CHANGE_THIS_TO_RANDOM_STRING_AT_LEAST_32_CHARS
JWT_EXPIRES_IN=24h

# ----------------------------------------------------------------------------
# CORS 설정
# ----------------------------------------------------------------------------
# 허용할 도메인 (쉼표로 구분)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ----------------------------------------------------------------------------
# 로그 설정
# ----------------------------------------------------------------------------
LOG_LEVEL=info
LOG_FILE=logs/app.log

# ----------------------------------------------------------------------------
# 백업 설정 (향후 사용)
# ----------------------------------------------------------------------------
BACKUP_ENABLED=true
BACKUP_SCHEDULE=0 2 * * *
BACKUP_PATH=C:\Backups\YmhDB

# ============================================================================
# 설치 후 확인사항
# ============================================================================
#
# □ DB_PASSWORD를 강력한 비밀번호로 변경했는가?
# □ SESSION_SECRET을 랜덤 문자열로 변경했는가?
# □ COMPANY_NAME을 실제 회사명으로 변경했는가?
# □ .env 파일 권한을 제한했는가?
#
# ============================================================================
